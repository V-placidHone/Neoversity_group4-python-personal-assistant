# Aggregates global search across entities:
#  - combines results from ContactsService and NotesService
#  - returns unified SearchHit objects (type + preview)


class SearchService:
    # def __init__(self, contacts: ContactsService, notes: NotesService):
    #     self.contacts = contacts
    #     self.notes = notes
    pass
